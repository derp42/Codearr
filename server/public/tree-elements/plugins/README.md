# Tree Element Plugins

Drop-in element definitions can be added here.

Example plugin (ES module):

```js
export default {
  type: "example",
  label: "Example",
  outputs: [{ id: "out", label: "out" }],
};
```
